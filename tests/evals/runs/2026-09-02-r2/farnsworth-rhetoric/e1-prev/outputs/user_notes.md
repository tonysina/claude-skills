# User Notes

## Uncertainty
- The original leaves exactly thirty seconds of lag undefined ("less than thirty" vs. "exceeds thirty"). I preserved the gap rather than closing it. The author needs to pick a side.
- "Should undertake verification" and "promotion may proceed" became imperatives ("check", "promote"). That is standard runbook mode but is a small modality shift from the original's "should"/"may". Flagged in the claim check.
- "Significant lag" is kept as written. The original never defines it; the reader can infer it means over thirty seconds, but I did not substitute the number.

## Needs Human Review
- Paragraph one ends on "operations wiki", which fails the Saxon-finish check. I chose to leave the reference pointer at the end rather than reorder the sentence, since in a runbook the pointer matters more than the cadence. A reviewer may prefer a different ordering.
- The prompt ("make this land") is a strengthen trigger, but the input is process text. I followed the register table and applied zero figures, and told the user why. If the eval expected figures to be applied, this run will not show them.

## Workarounds
- None. The skill's register table handled the routing cleanly.

## Suggestions
- The skill's description says "Do NOT use for ... process instructions" but the register table inside the skill says to proceed with diction-only work. Those two are slightly at odds: the description implies decline, the table implies do the free work. I followed the table since it is the more specific instruction and it produces useful output. The skill could state explicitly that for process/technical text the deliverable is a diction-only pass plus a note, rather than a refusal.
- For a runbook, the biggest readability win is structural (numbered steps, callout for the warning). The skill correctly declares restructuring out of scope, but it could name which skill or approach to hand off to for that case.
