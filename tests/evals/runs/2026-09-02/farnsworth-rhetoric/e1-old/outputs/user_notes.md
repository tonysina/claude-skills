# User Notes

## Uncertainty
- The skill lists "Process instructions" under "Avoid in", and this input is a runbook. The skill does not say whether that means decline, apply lightly, or apply only certain techniques. I chose to apply only Saxon word choice and Saxon finish and to skip every repetition figure. Another executor could have read the skill's "Full Example" (which uses anaphora and rule of three freely) as license to do the same to the runbook.
- "Make this land" is not one of the skill's listed triggers. I treated it as a synonym for "strengthen this".
- The 117 to 77 word counts in the response were verified with wc after an initial hand count came out wrong. Headings are excluded from both figures.

## Needs Human Review
- The rewrite says "under thirty seconds" and "over thirty seconds", matching the original's "less than" and "exceeds". Exactly thirty seconds is undefined in both. A database engineer should confirm which branch applies at the boundary before this ships.
- "You lose data" is a slightly stronger, more direct rendering of "will result in data loss". The claim is the same, but someone who owns the runbook should confirm the tone is acceptable for their audience.
- I changed "the on-call engineer should" to a direct imperative ("check", "promote", "escalate"). This assumes the reader is always the on-call engineer, which the original implies but does not state.

## Workarounds
- None. The skill loaded cleanly with no referenced files.

## Suggestions
- The skill should say what to do when a request falls in the "Avoid in" list. Options: decline and redirect, or apply only Techniques 1 and 2 (Saxon vocabulary and Saxon finish) and skip all repetition and reversal figures. As written, an executor has to infer this.
- The skill has no dosage guidance beyond "use sparingly". A concrete budget (for example, at most one figure per paragraph, none in procedural text) would prevent over-application.
- The skill has no check that the rewrite preserved factual content. For a runbook, a "no claim or threshold may change" rule would be valuable. I applied one on my own initiative.
- Consider adding "make this land" to the trigger list, since it is a natural way users ask for this kind of edit.
